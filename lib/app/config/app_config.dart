class AppConfig {
  /// Toggle to enable/disable in-app booking.
  /// In "Studio-First" mode, this might be set to false to encourage
  /// booking via the studio kiosk.
  static const bool allowInAppBooking = true;

  /// App Version
  static const String version = '1.0.0';
}
